{
  "name": "reserve-rec-api",
  "version": "0.1.0",
  "bin": {
    "app": "bin/app.js"
  },
  "scripts": {
    "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
    "cdk": "cdk",
    "test": "jest",
    "synth:local": "cdk synth -c @context=local --all",
    "build:admin": "node ./scripts/generate-sam-templates.js",
    "synth:dev": "cdk synth -c @context=dev --all",
    "deploy:dev": "cdk deploy -c @context=dev --all",
    "run:admin": "sam local start-api -t cdk.out/sam-ReserveRecApi-Local-AdminApiStack-AdminApi.yaml --env-vars env.json --warm-containers LAZY --skip-pull-image 2>&1 | tr '\r' '\n'",
    "run:admin:full": "yarn synth:local && yarn build:admin && yarn run:admin"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "dependencies": {
    "@aws-crypto/sha256-js": "^5.2.0",
    "@aws-sdk/client-cognito-identity": "^3.913.0",
    "@aws-sdk/client-cognito-identity-provider": "^3.913.0",
    "@aws-sdk/client-dynamodb": "^3.914.0",
    "@aws-sdk/client-secrets-manager": "^3.910.0",
    "@aws-sdk/client-ssm": "^3.910.0",
    "@aws-sdk/client-sts": "^3.913.0",
    "@aws-sdk/util-dynamodb": "^3.914.0",
    "@opensearch-project/opensearch": "^3.5.1",
    "@smithy/protocol-http": "^5.3.3",
    "@smithy/signature-v4": "^5.3.3",
    "aws-cdk": "^2.1030.0",
    "aws-cdk-lib": "^2.220.0",
    "aws-sdk": "^2.1692.0",
    "aws4": "^1.13.2",
    "axios": "^1.12.2",
    "constructs": "^10.0.0",
    "fs-extra": "^11.3.2",
    "https": "^1.0.0",
    "node-fetch": "2",
    "winston": "^3.17.0",
    "yargs": "^18.0.0"
  }
}
